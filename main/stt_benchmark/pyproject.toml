[project]
name = "stt-benchmark"
version = "0.1.0"
description = "End-to-end STT model benchmarking pipeline"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v"
